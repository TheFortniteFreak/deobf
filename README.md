# Usage
```bash
python Main/Main.py <lua_file> [--preset ...]
```

# Presets
`fixv` `parse` `pretty` `rdead` `rcom`
# Settings
`nointernet` - Turns off any apis

# Example

```bash
python Main/Main.py test.lua --pretty --parse --nointernet
```
